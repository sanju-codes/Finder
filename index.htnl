<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="theme-color" content="#2563eb" />
  <title>Finder - Near Me</title>

  <link rel="manifest" href="manifest.json" />
  <style>
    :root{
      --bg:#f4f7fb;
      --card:#ffffff;
      --text:#0f172a;
      --muted:#64748b;
      --primary:#2563eb;
      --primary2:#0ea5e9;
      --shadow:0 10px 30px rgba(15,23,42,.10);
      --radius:22px;
      --radius-sm:16px;
    }

    *{box-sizing:border-box}
    body{
      margin:0;
      font-family:system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Arial,sans-serif;
      background:var(--bg);
      color:var(--text);
    }

    .app{
      max-width:520px;
      margin:0 auto;
      min-height:100vh;
      padding:14px 14px 26px;
    }

    .top-banner{
      background:linear-gradient(135deg,var(--primary),var(--primary2));
      color:#fff;
      border-radius:28px;
      padding:18px;
      box-shadow:var(--shadow);
      position:relative;
      overflow:hidden;
    }

    .top-banner::after{
      content:"";
      position:absolute;
      inset:auto -40px -50px auto;
      width:170px;
      height:170px;
      background:rgba(255,255,255,.10);
      border-radius:50%;
    }

    .brand-row{
      display:flex;
      justify-content:space-between;
      align-items:flex-start;
      gap:12px;
      position:relative;
      z-index:1;
    }

    .brand h1{
      margin:0;
      font-size:30px;
      line-height:1;
      letter-spacing:.2px;
    }

    .brand p{
      margin:8px 0 0;
      font-size:14px;
      opacity:.92;
    }

    .mini-pill{
      background:rgba(255,255,255,.16);
      border:1px solid rgba(255,255,255,.18);
      padding:10px 12px;
      border-radius:999px;
      font-size:12px;
      white-space:nowrap;
      backdrop-filter:blur(10px);
    }

    .status-card{
      margin-top:14px;
      background:rgba(255,255,255,.14);
      border:1px solid rgba(255,255,255,.16);
      border-radius:20px;
      padding:14px;
      display:grid;
      gap:8px;
      position:relative;
      z-index:1;
      backdrop-filter:blur(10px);
    }

    .status-line{
      display:flex;
      justify-content:space-between;
      gap:12px;
      align-items:flex-start;
      flex-wrap:wrap;
    }

    .location{
      font-size:15px;
      font-weight:600;
    }

    .weather{
      text-align:right;
      font-size:14px;
      line-height:1.35;
    }

    .weather strong{
      display:block;
      font-size:22px;
      margin-bottom:2px;
    }

    .ad-slot{
      margin:14px 0;
      background:linear-gradient(180deg,#fff,#f8fbff);
      border:1px dashed #cbd5e1;
      border-radius:20px;
      padding:14px;
      box-shadow:var(--shadow);
    }

    .ad-label{
      font-size:11px;
      letter-spacing:.12em;
      text-transform:uppercase;
      color:#64748b;
      margin-bottom:8px;
    }

    .ad-box{
      min-height:70px;
      border-radius:16px;
      display:flex;
      align-items:center;
      justify-content:center;
      text-align:center;
      color:#475569;
      background:repeating-linear-gradient(
        45deg,
        #eef2ff,
        #eef2ff 12px,
        #f8fafc 12px,
        #f8fafc 24px
      );
      font-size:14px;
      padding:12px;
    }

    .section-title{
      display:flex;
      justify-content:space-between;
      align-items:center;
      margin:18px 4px 10px;
    }

    .section-title h2{
      margin:0;
      font-size:17px;
    }

    .section-title span{
      color:var(--muted);
      font-size:13px;
    }

    .facilities-grid{
      display:grid;
      grid-template-columns:repeat(2,minmax(0,1fr));
      gap:12px;
    }

    .card{
      border:none;
      background:var(--card);
      border-radius:22px;
      padding:16px;
      box-shadow:var(--shadow);
      text-align:left;
      color:var(--text);
      display:flex;
      gap:12px;
      align-items:center;
      min-height:86px;
      cursor:pointer;
      transition:transform .2s ease, box-shadow .2s ease;
    }

    .card:active{transform:scale(.98)}
    .card:hover{transform:translateY(-2px)}
    .icon{
      width:48px;
      height:48px;
      border-radius:16px;
      display:grid;
      place-items:center;
      font-size:22px;
      flex:0 0 auto;
      background:linear-gradient(135deg,#dbeafe,#eff6ff);
    }

    .card h3{
      margin:0;
      font-size:15px;
      line-height:1.25;
    }

    .card p{
      margin:4px 0 0;
      color:var(--muted);
      font-size:12px;
      line-height:1.35;
    }

    .quick-bar{
      margin-top:14px;
      display:grid;
      grid-template-columns:repeat(3,1fr);
      gap:10px;
    }

    .quick-btn{
      border:none;
      background:#fff;
      border-radius:18px;
      padding:12px 10px;
      box-shadow:var(--shadow);
      font-size:12px;
      color:var(--text);
      cursor:pointer;
    }

    .footer-ad{
      margin-top:16px;
    }

    .footer{
      margin-top:18px;
      text-align:center;
      font-size:12px;
      color:var(--muted);
      padding-bottom:8px;
    }

    @media (max-width:420px){
      .facilities-grid{grid-template-columns:1fr}
      .brand h1{font-size:28px}
      .weather strong{font-size:20px}
    }
  </style>
</head>
<body>
  <div class="app">
    <section class="top-banner">
      <div class="brand-row">
        <div class="brand">
          <h1>Finder</h1>
          <p>Find everything near you in one tap</p>
        </div>
        <div class="mini-pill" id="timePill">Loading...</div>
      </div>

      <div class="status-card">
        <div class="status-line">
          <div class="location" id="locationText">📍 Detecting your location...</div>
          <div class="weather" id="weatherText">
            <strong>--°</strong>
            Weather loading...
          </div>
        </div>
      </div>
    </section>

    <div class="ad-slot">
      <div class="ad-label">Sponsored</div>
      <div class="ad-box">
        Top Banner Ad Space<br />
        Replace this box with AdSense or your ad code
      </div>
    </div>

    <div class="section-title">
      <h2>Facilities</h2>
      <span>Tap to open nearby places</span>
    </div>

    <div class="facilities-grid">
      <button class="card" onclick="openNearby('hotel')">
        <div class="icon">🏨</div>
        <div>
          <h3>Hotels Near Me</h3>
          <p>Nearby rooms, stays, and lodges</p>
        </div>
      </button>

      <button class="card" onclick="openNearby('fuel station')">
        <div class="icon">⛽</div>
        <div>
          <h3>Fuel Stations Near Me</h3>
          <p>Petrol, diesel, and nearby pumps</p>
        </div>
      </button>

      <button class="card" onclick="openNearby('ATM')">
        <div class="icon">🏧</div>
        <div>
          <h3>ATMs Near Me</h3>
          <p>Nearby cash withdrawal points</p>
        </div>
      </button>

      <button class="card" onclick="openNearby('hospital')">
        <div class="icon">🏥</div>
        <div>
          <h3>Hospitals Near Me</h3>
          <p>Clinics, emergency care, and hospitals</p>
        </div>
      </button>

      <button class="card" onclick="openNearby('medical shop')">
        <div class="icon">💊</div>
        <div>
          <h3>Medical Shops Near Me</h3>
          <p>Pharmacies and medicine stores</p>
        </div>
      </button>

      <button class="card" onclick="openNearby('bus stop')">
        <div class="icon">🚏</div>
        <div>
          <h3>Bus Stops Near Me</h3>
          <p>Find the nearest bus stops easily</p>
        </div>
      </button>

      <button class="card" onclick="openNearby('akshaya center')">
        <div class="icon">🏢</div>
        <div>
          <h3>Akshaya Centers Near Me</h3>
          <p>Service centers and e-governance help</p>
        </div>
      </button>

      <button class="card" onclick="refreshLocation()">
        <div class="icon">📍</div>
        <div>
          <h3>Current Location</h3>
          <p>Refresh GPS and update weather</p>
        </div>
      </button>
    </div>

    <div class="ad-slot footer-ad">
      <div class="ad-label">Sponsored</div>
      <div class="ad-box">
        Middle / Bottom Ad Space<br />
        You can place a 320x100 banner here
      </div>
    </div>

    <div class="quick-bar">
      <button class="quick-btn" onclick="refreshLocation()">Refresh</button>
      <button class="quick-btn" onclick="openGoogleMaps()">Maps</button>
      <button class="quick-btn" onclick="shareApp()">Share</button>
    </div>

    <div class="footer">GitHub Pages PWA ready</div>
  </div>

  <script>
    let currentLat = null;
    let currentLng = null;

    const locationText = document.getElementById("locationText");
    const weatherText = document.getElementById("weatherText");
    const timePill = document.getElementById("timePill");

    function updateClock() {
      const now = new Date();
      timePill.textContent = now.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
    }
    updateClock();
    setInterval(updateClock, 60000);

    function refreshLocation() {
      if (!navigator.geolocation) {
        locationText.textContent = "📍 Geolocation not supported";
        return;
      }

      locationText.textContent = "📍 Detecting your location...";

      navigator.geolocation.getCurrentPosition(
        async (pos) => {
          currentLat = pos.coords.latitude;
          currentLng = pos.coords.longitude;

          try {
            const geoRes = await fetch(`https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat=${currentLat}&lon=${currentLng}`);
            const geoData = await geoRes.json();
            const place = geoData.address || {};
            const city = place.city || place.town || place.village || place.state_district || place.state || "Your area";
            const state = place.state || "";
            locationText.textContent = `📍 ${city}${state ? ", " + state : ""}`;
          } catch (e) {
            locationText.textContent = `📍 ${currentLat.toFixed(4)}, ${currentLng.toFixed(4)}`;
          }

          loadWeather(currentLat, currentLng);
        },
        () => {
          locationText.textContent = "📍 Location permission denied";
          weatherText.innerHTML = "<strong>--°</strong> Weather unavailable";
        },
        { enableHighAccuracy: true, timeout: 10000, maximumAge: 60000 }
      );
    }

    async function loadWeather(lat, lon) {
      try {
        const res = await fetch(
          `https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&current_weather=true`
        );
        const data = await res.json();
        const w = data.current_weather;
        if (w) {
          weatherText.innerHTML = `<strong>${Math.round(w.temperature)}°C</strong>Live weather`;
        } else {
          weatherText.innerHTML = "<strong>--°</strong> Weather unavailable";
        }
      } catch (e) {
        weatherText.innerHTML = "<strong>--°</strong> Weather unavailable";
      }
    }

    function openNearby(query) {
      if (currentLat !== null && currentLng !== null) {
        window.open(
          `https://www.google.com/maps/search/${encodeURIComponent(query)}/@${currentLat},${currentLng},15z`,
          "_blank"
        );
      } else {
        window.open(`https://www.google.com/maps/search/${encodeURIComponent(query)}`, "_blank");
      }
    }

    function openGoogleMaps() {
      if (currentLat !== null && currentLng !== null) {
        window.open(`https://www.google.com/maps/@${currentLat},${currentLng},15z`, "_blank");
      } else {
        window.open("https://www.google.com/maps", "_blank");
      }
    }

    function shareApp() {
      const shareData = {
        title: "Finder",
        text: "Find hotels, fuel stations, ATMs, hospitals and more near you.",
        url: window.location.href
      };

      if (navigator.share) {
        navigator.share(shareData);
      } else {
        navigator.clipboard.writeText(window.location.href);
        alert("Link copied to clipboard");
      }
    }

    refreshLocation();

    if ("serviceWorker" in navigator) {
      window.addEventListener("load", () => {
        navigator.serviceWorker.register("service-worker.js");
      });
    }
  </script>
</body>
</html>


